# Repository workflow

- After completing and verifying each requested code or documentation change, automatically stage only the files related to that request, create a concise commit, and push the current branch to `origin`.
- Do not include unrelated or pre-existing working-tree changes in the commit.
- If committing or pushing is blocked, report the blocker and the exact remaining action instead of silently skipping publication.
